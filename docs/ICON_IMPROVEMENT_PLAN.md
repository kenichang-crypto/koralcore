# 圖標顯示提升計劃

## 當前狀態分析

### 統計數據
- **reef-b-app ic_* 圖標**: 85 個
- **koralcore ic_* 圖標**: 56 個（SVG + PNG）
- **完成度**: 約 66% (56/85)
- **剩餘 errorBuilder**: 4 處

---

## 發現的問題

### 1. ⚠️ 剩餘 errorBuilder（4 處）

1. **splash_page.dart** (2 處)
   - Splash logo 的 fallback
   - 狀態: 可優化，但保留作為安全措施

2. **device_card.dart** (1 處)
   - 設備圖標的 fallback
   - 狀態: 可優化，但保留作為安全措施

3. **dosing_main_page.dart** (1 處)
   - 動態圖標的 fallback
   - 狀態: 需要檢查

---

### 2. ⚠️ 缺失的圖標

從對比分析發現，以下圖標在 reef-b-app 中存在但 koralcore 中缺失：

**高優先級（常用圖標）**:
- `ic_add_btn` - 添加按鈕
- `ic_add_rounded` - 圓角添加按鈕
- `ic_add_white` - 白色添加按鈕
- `ic_blue_light_thumb` - 藍光縮略圖
- `ic_cold_white_light_thumb` - 冷白光縮略圖
- `ic_green_light_thumb` - 綠光縮略圖
- `ic_friday_select/unselect` - 週五選擇器
- `ic_green_check` - 綠色勾選

**中優先級（特定功能圖標）**:
- 其他特定功能的圖標

---

### 3. ⚠️ placeholderBuilder 使用

在 `scene_icon_helper.dart` 中發現使用 `placeholderBuilder`，這可能導致圖標加載時顯示 fallback。

---

## 提升計劃

### 階段 1: 優化現有 errorBuilder（立即）

**目標**: 確保所有 errorBuilder 都是必要的，並優化 fallback 顯示

1. **splash_page.dart**
   - 檢查 splash logo 是否存在
   - 優化 fallback 顯示

2. **device_card.dart**
   - 檢查設備圖標是否存在
   - 優化 fallback 顯示

3. **dosing_main_page.dart**
   - 檢查動態圖標路徑
   - 優化 fallback 顯示

---

### 階段 2: 補齊高優先級缺失圖標（短期）

**目標**: 補齊最常用的缺失圖標

1. **添加按鈕圖標** (3 個)
   - `ic_add_btn.svg`
   - `ic_add_rounded.svg`
   - `ic_add_white.svg`

2. **光色縮略圖** (3 個)
   - `ic_blue_light_thumb.svg`
   - `ic_cold_white_light_thumb.svg`
   - `ic_green_light_thumb.svg`

3. **其他常用圖標** (2 個)
   - `ic_green_check.svg`
   - `ic_friday_select.svg` / `ic_friday_unselect.svg`

---

### 階段 3: 優化 placeholderBuilder（中期）

**目標**: 移除不必要的 placeholderBuilder，確保圖標能立即顯示

1. **scene_icon_helper.dart**
   - 檢查所有場景圖標是否存在
   - 移除不必要的 placeholderBuilder

---

### 階段 4: 補齊所有缺失圖標（長期）

**目標**: 達到 100% 圖標對照

1. 系統性對比所有圖標
2. 轉換所有缺失的 XML drawable 為 SVG
3. 更新 CommonIconHelper 添加新圖標方法

---

## 預期結果

### 完成後
- **errorBuilder**: 僅保留必要的（splash 和設備圖標）
- **缺失圖標**: 從 29 個減少到 < 10 個
- **完成度**: 從 66% 提升到 **85%+**
- **placeholderBuilder**: 移除或優化

---

## 優先級

### 🔴 高優先級（立即處理）
1. 優化現有 errorBuilder
2. 補齊添加按鈕圖標（3 個）

### 🟡 中優先級（短期處理）
3. 補齊光色縮略圖（3 個）
4. 優化 placeholderBuilder

### 🟢 低優先級（長期處理）
5. 補齊所有剩餘圖標

