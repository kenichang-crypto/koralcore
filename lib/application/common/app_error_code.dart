/// Canonical set of application-level error codes exposed to callers.
enum AppErrorCode {
  deviceBusy,
  noActiveDevice,
  noDeviceSelected,
  notSupported,
  invalidParam,
  transportError,
  unknownError,
}
