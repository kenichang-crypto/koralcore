# LED 字符串遷移進度

## 已完成的更新

### 預覽相關字符串
- ✅ `ledRecordsSnackPreviewStarted`: 已更新為 "One-minute quick preview started." (對照 `toast_preview_start`)
- ✅ `ledRecordsSnackPreviewStopped`: 已更新為 "One-minute quick preview ended." (對照 `toast_preview_end`)
- ✅ 繁體中文已同步更新

### 錯誤消息字符串
- ✅ `ledRecordSettingErrorSunTime`: 已更新為 "Sunrise and sunset time settings are incorrect." (對照 `toast_sun_time_error`)
- ✅ 繁體中文需要更新

### 場景限制字符串
- ✅ `ledSceneLimitReached`: 已更新為 "Maximum 5 custom scenes can be set." (對照 `toast_custom_scene_size_more_than_5`)
- ✅ 繁體中文已同步更新

## 待處理的字符串

### Activity 標題字符串
- [ ] `activity_led_setting_title` → 需要對照
- [ ] `activity_led_master_setting_title` → 需要對照
- [ ] `activity_led_record_setting_title` → 需要對照
- [ ] `activity_led_record_title` → 需要對照
- [ ] `activity_led_record_time_setting_title` → 需要對照
- [ ] `activity_led_scene_title` → 需要對照
- [ ] `activity_led_scene_add_title` → 需要對照
- [ ] `activity_led_scene_edit_title` → 需要對照
- [ ] `activity_led_scene_delete_title` → 需要對照

### Dialog 字符串
- [ ] `dialog_led_move_master_title` → 需要對照
- [ ] `dialog_led_move_master_content` → 需要對照
- [ ] `dialog_led_delete_time_content` → 需要對照
- [ ] `dialog_led_clear_record_content` → 需要對照
- [ ] `dialog_scene_delete_scene_content` → 需要對照

### Toast 字符串
- [ ] `toast_add_scene_successful` → 需要對照
- [ ] `toast_scene_name_is_exist` → 需要對照
- [ ] `toast_delete_scene_successful` → 需要對照
- [ ] `toast_delete_scene_failed` → 需要對照
- [ ] `toast_delete_record_successful` → 需要對照
- [ ] `toast_delete_record_failed` → 需要對照

### 其他字符串
- [ ] `time_already_exist` → 需要對照
- [ ] `init_strength` → 需要對照（可能已存在為 `ledRecordSettingInitStrength`）
- [ ] `sunrise` → 需要對照（可能已存在為 `ledRecordSettingSunrise`）
- [ ] `sunset` → 需要對照（可能已存在為 `ledRecordSettingSunset`）
- [ ] `slow_start` → 需要對照（可能已存在為 `ledRecordSettingSlowStart`）
- [ ] `moon_light` → 需要對照（可能已存在為 `ledRecordSettingMoonlight`）

## 下一步

1. 繼續添加缺失的字符串到所有語言的 ARB 文件
2. 處理圖標遷移
3. 重新生成本地化文件並驗證

