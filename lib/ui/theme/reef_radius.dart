/// Corner radii extracted from reef-b shapes and button specs.
class ReefRadius {
  const ReefRadius._();

  static const double none = 0;
  static const double xs = 4; // TextInput + cards
  static const double sm = 6; // Switch track overlay
  static const double md = 10; // Dialogs / sheets
  static const double lg = 20; // Rounded buttons
  static const double pill = 999; // Full pill / round surfaces
}
